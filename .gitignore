.env
.venv/
venv/
__pycache__/
*.pyc
node_modules/
.next/
data/test/
data/_demo_staging/
backend/outputs/
# Runtime upload dir: only generated per-analysis job folders live here.
backend/uploads/
*.tar
*.tar.gz
# Large dataset archives (xView2) — keep on disk for fine-tuning, never commit.
*.zip
backend/.venv/
# Upstream clones and trained weights: large, regenerable, never committed.
ml/xview2-baseline/
ml/pytorch-xview2/
ml/checkpoints/*.ckpt
# Regenerable caches
*.tsbuildinfo
.pytest_cache/
